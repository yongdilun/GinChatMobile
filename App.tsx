// This file is the entry point for the Expo app
// We're using expo-router, so we just need to export a placeholder component
// The actual app is defined in the app/ directory

import React from 'react';
import { View } from 'react-native';

// This is just a placeholder component
// The real app is loaded through expo-router in the app/ directory
export default function App() {
  return <View />;
}
